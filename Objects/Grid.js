import Dot from './Dot.js';

class Grid {
  constructor() {
    this.makeGrid();
  }

  makeGrid() {
    this.grid = [];
    for (let yPos = 0; yPos < 7; yPos++) {
      let dotRow = [];
      for (let xPos = 0; xPos < 7, yPos++) {

      }
    }
  }
}
